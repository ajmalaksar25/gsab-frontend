# Install

GSAB requires Python 3.9+.

```bash
pip install gsab              # core
pip install "gsab[pandas]"    # + DataFrame support (to_dataframe / from_dataframe)
```

That's the whole install. Next, sign in once — see [Authentication](/docs/authentication).
